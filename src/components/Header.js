import React from "react";

function Header(){

  const headerStyle = {
    color: "white"
  }

  return (
    <div style={headerStyle}>
      <h1>Tap Room</h1>
    </div>
  );
}

export default Header;